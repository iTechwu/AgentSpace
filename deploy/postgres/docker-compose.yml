services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: dofe_agent
      POSTGRES_USER: dofe_agent
      POSTGRES_PASSWORD: dofe_agent
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
