DOFE_AGENT_SERVER_URL=http://host.docker.internal:1455
DOFE_AGENT_DAEMON_TOKEN=replace-with-one-time-daemon-token
DOFE_AGENT_DAEMON_ID=local-docker-managed-node
DOFE_AGENT_DEVICE_NAME=Local Docker
DOFE_AGENT_RUNTIME_NAME=Managed execution node
MANAGED_NODE_STATE_DIR=/absolute/host/path/to/managed-node-state
MANAGED_NODE_USER=0:0
# Absolute path to the CA that signs DOFE_AGENT_SERVER_URL when it is private
# (for example, a test environment using mkcert). Leave empty for public TLS.
MANAGED_NODE_TLS_CA_PATH=
# Optional connectivity passed to dynamically created provider containers. Use
# host-gateway only when the model endpoint resolves to the Docker host, e.g.
# model.local.dofe.ai:host-gateway for the local Models API.
MANAGED_RUNTIME_DOCKER_EXTRA_HOSTS=
# Absolute host path of the CA used by provider-container gateway requests.
MANAGED_RUNTIME_TLS_CA_PATH=
MANAGED_RUNTIME_DOCKER_NETWORK=dofe-runtime-restricted
# Runtime app installation uses a separate ephemeral-container network. Apply
# registry/firewall allow-lists to this network; task containers never join it.
MANAGED_RUNTIME_INSTALL_DOCKER_NETWORK=dofe-managed-install
DOFE_AGENT_NPM_REGISTRY=https://registry.npmjs.org
DOFE_AGENT_PYPI_INDEX_URL=https://pypi.org/simple
MANAGED_RUNTIME_IMAGE_TAG=latest
# Immutable, operator-approved images used only by the task-scoped Skill Runner.
# Script installations stay blocked while the matching runtime image is empty.
DOFE_SKILL_RUNNER_NODE_IMAGE=
DOFE_SKILL_RUNNER_PYTHON_IMAGE=
DOFE_SKILL_RUNNER_BASH_IMAGE=
# Optional execution timeout; values above 600000 are capped at ten minutes.
DOFE_SKILL_RUNNER_TIMEOUT_MS=60000
# Comma-separated HTTPS origins allowed for immutable Skill blob downloads.
# Include scheme and optional port, but no path/query/credentials.
DOFE_AGENT_SKILL_ARTIFACT_DOWNLOAD_ORIGINS=https://attachments.example.com
# Private/special-address origins are denied unless also listed here. Keep empty
# for public object storage; use only for audited internal HTTPS blob proxies.
DOFE_AGENT_SKILL_ARTIFACT_PRIVATE_DOWNLOAD_ORIGINS=

# MCP egress proxy canary. Set true only after the process-isolation and real
# container negative-probe release gates in docs/0803/mcp-egress-proxy pass.
# When MCP_EGRESS_ENFORCE=true the managed node reconciles DOCKER-USER rules
# before starting any Runtime. The Runtime subnet and allowed endpoints must be
# supplied explicitly; proxy/container IPs are resolved at reconcile time.
MCP_EGRESS_ENFORCE=false
MCP_EGRESS_PROXY_URL=http://172.31.240.2:8080
MCP_EGRESS_PROXY_ADMIN_TOKEN=replace-with-active-proxy-admin-token
RUNTIME_SUBNET=172.31.240.0/24
PROXY_RUNTIME_IP=172.31.240.2
CONTROL_PLANE_IPV4=
MODELS_GATEWAY_IPV4=
DNS_RESOLVER_IPV4=
