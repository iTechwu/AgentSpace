# PostgreSQL.
SELF_HOSTED_DATABASE_URL=postgres://agent_space:agent_space@127.0.0.1:5432/agent_space
SELF_HOSTED_DATABASE_DIRECT_URL=postgres://agent_space:agent_space@127.0.0.1:5432/agent_space

# E2E and database integration tests must use a separate database.
AGENT_SPACE_TEST_DATABASE_URL=postgres://agent_space:agent_space@127.0.0.1:5432/agent_space_test

# Local attachment fallback. TOS is used automatically when its credentials are configured.
SELF_HOSTED_ATTACHMENT_LOCAL_ROOT=/var/lib/agentspace/workspaces

# Volcengine TOS, using its S3-compatible API. Keep credentials only in the
# deployment secret store. The public endpoint is used by default so local
# development works. Enable TOS_USE_INTERNAL_ENDPOINT only inside a Volcengine VPC.
TOS_BUCKET=replace_with_tos_bucket
TOS_REGION=cn-beijing
TOS_ENDPOINT=https://tos-<region>.volces.com
TOS_INTERNAL_ENDPOINT=
# Legacy S3-compatible endpoint aliases are accepted and normalized automatically.
# TOS_S3_ENDPOINT=
# TOS_INTERNAL_S3_ENDPOINT=
TOS_USE_INTERNAL_ENDPOINT=false
# Optional custom domain for signed browser download URLs.
TOS_BUCKET_DOMAIN=
TOS_ACCESS_KEY=replace_with_tos_access_key
TOS_SECRET_KEY=replace_with_tos_secret_key

# Attachment download and upload policy.
# Set to local for isolated tests or an emergency local-only deployment.
# ATTACHMENT_STORAGE_PROVIDER=local
ATTACHMENT_SIGNED_URL_TTL_SECONDS=300
ATTACHMENT_MAX_UPLOAD_BYTES=52428800
# Local attachment fallback.
ATTACHMENT_LOCAL_ROOT=${SELF_HOSTED_ATTACHMENT_LOCAL_ROOT}
ATTACHMENT_ENABLE_LOCAL_FALLBACK=true

# AgentSpace web app base URL
AGENT_SPACE_APP_URL=http://127.0.0.1:1455

# Dofe SSO is the only user identity and login provider. Configure these values
# in your deployment secret store; never commit SSO_CLIENT_SECRET or
# INTERNAL_API_SECRET.
SSO_API_URL=https://sso.ixicai.cn/api
SSO_INTERNAL_API_URL=https://sso.ixicai.cn/api
SSO_ISSUER=https://sso.ixicai.cn/api
SSO_CLIENT_ID=agentspace-dofe-ai-dev
SSO_CLIENT_SECRET=replace_with_sso_client_secret
SSO_SERVICE_NAME=agentspace.local.dofe.ai
INTERNAL_API_SECRET=replace_with_sso_internal_api_secret
SSO_REDIRECT_URI=https://agentspace.local.dofe.ai/auth/callback
JWKS_PATH=.well-known/jwks.json
JWKS_URI=https://sso.ixicai.cn/api/.well-known/jwks.json
INTERNAL_JWKS_URI=https://sso.ixicai.cn/api/.well-known/jwks.json
SSO_DISCOVERY_URL=https://sso.ixicai.cn/api/.well-known/openid-configuration

# Required for self-hosted production builds that use Next.js Server Actions.
# Use the same value for every web instance and every build of the same deployment.
# Generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=replace_with_base64_encoded_32_byte_key

# Used to sign OAuth state / nonce cookies
AGENT_SPACE_OAUTH_STATE_SECRET=replace_with_a_long_random_hex_string

# Optional: configure Google Workspace / Google Sheets integration.
# See GOOGLE_WORKSPACE_SETUP.md.
AGENT_SPACE_GOOGLE_WORKSPACE_CLIENT_ID=replace_with_google_workspace_client_id
AGENT_SPACE_GOOGLE_WORKSPACE_CLIENT_SECRET=replace_with_google_workspace_client_secret
AGENT_SPACE_GOOGLE_WORKSPACE_CALLBACK_URL=http://127.0.0.1:1455/api/integrations/google/callback
# Generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AGENT_SPACE_GOOGLE_WORKSPACE_TOKEN_ENCRYPTION_KEY=replace_with_base64_encoded_32_byte_key
# Optional Drive folder id for newly created Google Sheets.
AGENT_SPACE_GOOGLE_DRIVE_PARENT_FOLDER_ID=
