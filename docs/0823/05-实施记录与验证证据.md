# DeepSeek Harness Runtime 实施记录与验证证据

## 1. 实施基线

- 分支：`techwu/deepseek-harness-runtime`（已合并到 `dev`，合并提交 `1e8af517`，2026-08-24）
- AgentSpace 基线：`13b71505`
- DeepSeek Harness checkout：`dsh-v0.1.1-rc.2`（`b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`）
- DeepSeek Harness origin：`git@github.com:iTechwu/deepseek-harness.git`
- npm 包：`@deepseek-ai/dsh@0.1.1-rc.2`
- npm integrity：`sha512-UP1UIh6q3Gme/yXRn/QL2P8IsVlv8Shpg22TRJIZPsCRWLm4CBiA1MUvXmJAfsOEETBMLAl+xWPtFw6ICsN3wg==`

## 2. 已提交改动

| Commit | 内容 |
| --- | --- |
| `2e81e38f` | AgentRouter adapter、provider runtime、凭据、健康检查和 provisioning 模板 |
| `adf2db63` | `deepseek_native` 本地模型目录、严格 allowlist、Web 创建入口和灰度开关 |
| `ff51b497` | runtime 镜像/Compose、隔离 env 模板、构建脚本和原生健康探针测试 |
| `290da606` | workspace-write 安全 overlay，关闭 telemetry、Web search 和未治理多 agent 工具 |
| `93676379` | DeepSeek auth/model/profile 诊断及 empty/timeout 回归测试 |
| `335f0cb1` | DeepSeek headless overlay 改为 UUID 临时文件，补充同目录并发隔离测试 |
| `f0fd09e3` | 将 `harness_started` 映射为 DeepSeek provider status 消息 |
| `b7397461` | 增加 24 小时旧 overlay 清理，覆盖 daemon 异常退出后的残留场景 |
| `515c2fcb` | 为每个 DeepSeek headless task 分配独立 `DSH_HOME` 并在退出后递归清理 |
| `2f274ca3` | 健康检查增加 `dsh --profile headless --help` 门禁和 profile 错误分类 |
| `629e3932` | 增加 DeepSeek `executeRemoteTask` smoke，覆盖 runtime-output 上传、complete 和 workdir cleanup |
| `966d7c76` | 增加任务启动前的 DeepSeek 已验证模型目录门禁 |
| `2b65f90f` | DeepSeek provider health broken 时停止新 task claim，并保留运维修复队列 |
| `7ecd682b` | 增加 DeepSeek 任务执行前健康门禁，覆盖 claim 后 health 竞态 |
| `e0901bc1` | 健康门禁使用固定安全错误文案，避免透传 provider 原始原因 |
| `b4c4d39c` | 补充 DeepSeek 灰度开关默认关闭与严格值校验测试 |
| `cd6019b7` | 让 DeepSeek 员工进入聊天 composer，并限制为 Runtime 默认权限 |
| `8ae23bcf` | 将托管运行时 provider health 从 metadata 映射到列表/详情，并展示不可用、降级和待验证原因 |
| `d26af379` | 员工执行引擎选项复用托管运行时 provider health，避免绑定界面丢失 DeepSeek 诊断 |
| `fb93705f` | 保持 offline 优先于 provider unusable，避免节点离线被误报为供应商故障 |
| `4b630c11` | UI 与服务端双重阻止 provider broken 的托管运行时绑定新员工 |
| `49da3715` | 模型覆盖与有效模型解析支持 DeepSeek 原生 runtime-local Flash/Pro 目录 |
| `c590f42d` | 用 runtime provider 协议补全 DeepSeek provider event 与网关 usage 的 `deepseek_native` 归属 |
| `d90910de` | 在员工执行引擎选项中标注 DeepSeek P0 headless 且不支持 resume |
| `184f0b1d` | provisioning 审计补充 DeepSeek provider、协议、默认模型和 allowlist 元数据 |
| `cc798f3f` | 在托管节点心跳中记录 DeepSeek provider health 状态跃迁，并过滤原始 provider message |
| `aeef9157` | provisioning 详情和 runtime 列表公开投影补齐 provider health，同时移除原始错误文本和错误消息 |
| `c8087e3a` | 统一脱敏 provider health reason/error 文本，限制长度并清理凭据形态值 |
| `37ec1221` | 扩展带引号 JSON 字段和 URL 查询参数的 provider health 凭据脱敏覆盖 |
| `cef72473` | MCP connection materialization 增加 Provider eligibility 服务端门禁 |
| `078f71a2` | MCP 任务投影、历史连接降级、session claim 和 gateway 校验统一 fail-closed，并复用同一 eligibility 函数 |
| `636e269e` | Provider 账户下拉补齐 DeepSeek canary 入口，创建账户与供给请求增加服务端 feature-flag 门禁 |

### 2.1 JSON-RPC one-shot 协议基础（2026-08-24，已随分支合并）

- 增加默认关闭的 DeepSeek JSON-RPC one-shot 协议基础、逐类型事件契约、进程退出梯子和 launch plan 释放钩子，并同步本文档集；补充 macOS `/var` canonical path 的跨平台断言。
- 该工作最初在桌面沙箱下无法提交（`.git` 仅读、无法创建 `.git/index.lock`）；随后已随 `techwu/deepseek-harness-runtime` 分支一并合并到 `dev`（合并提交 `1e8af517`），对应提交包括 `392602ea`（固化 JSON-RPC 协商版本与发布证据）、`94f3d999`（接入 DeepSeek 会话端点环境覆盖）、`74170792`（完善 DeepSeek 发布门禁与会话取消）等。

## 3. As-built 行为

1. `deepseek-harness` 已加入 `DaemonProvider`、AgentRouter harness 与 provider catalog。
2. 默认模型为 `deepseek-v4-flash`，可选 `deepseek-v4-pro`；其他模型在 runtime-local allowlist 阶段被拒绝。
3. 启动命令为 `dsh --profile headless --patch <overlay> <prompt>`，prompt 作为单一 argv 参数，不经过 shell 拼接。
4. 每个 task 使用 fresh headless session。直接传入 `sessionId` 返回 `harness.session_missing`；provider queue 路径记录 `provider_session_unsupported` 后启动 fresh session。
5. `DEEPSEEK_API_KEY`/`DEEPSEEK_BASE_URL` 进入 managed credential 白名单和脱敏清单；生产 key 来源是 Models `RuntimeCredential` 一次性 secret，launcher 不包含明文 secret。
6. health 先执行 `dsh --version`，再以 stdin 传递 header 的子进程请求 Models gateway 派生的 `${DEEPSEEK_BASE_URL}/models`；key 不进入 argv 或 metadata，发布 gate 拒绝直连 endpoint。
   对 DeepSeek 请求会解析非敏感的 model id 摘要，并要求 `deepseek-v4-flash` 与 `deepseek-v4-pro` 同时存在；缺失时返回 `provider.model_unavailable`，不会把 endpoint 2xx 误判为模型可用。
7. Web 创建入口默认关闭，设置 `NEXT_PUBLIC_DEEPSEEK_HARNESS_RUNTIME_ENABLED=1` 后显示 provider 与两个原生模型。
8. P0 overlay 固定 `workspace-write`，设置 `DSH_TELEMETRY_DISABLED=1`，禁用 Web search、subagent、workflow 和 Ralph。实时 tool event、resume 和 AgentSpace approval bridge 不在 P0 中。
9. 每次 headless 调用使用 `.dofe-deepseek-harness.patch-<uuid>.yml` 临时 overlay，并在成功、失败、超时或取消后清理；同一持久化会话工作目录中的并发调用不会覆盖彼此的模型配置。
10. AgentRouter 的 `harness_started` 事件会映射为 provider status 消息，远端任务可在 headless 进程启动后立即显示执行中状态；不会伪造实时文本或工具事件。
11. 新任务启动前只清理匹配命名且修改时间超过 24 小时的 DeepSeek overlay，用于回收 daemon 强制退出留下的残留文件；近期文件不会被误删。
12. 每个 headless task 使用独立 UUID `DSH_HOME`，即使同一持久化工作目录并发执行，也不会共享 DeepSeek settings/cache/session；任务结束后该目录递归清理。
13. DeepSeek 健康检查在模型目录 probe 前先执行 `dsh --profile headless --help`；版本可执行但 headless profile 失败时返回 `provider.profile_missing`，不会误标为 healthy。
14. `runProviderTask` 在已有健康快照时再次检查所选模型；若模型不在已验证目录中，返回 `provider.model_unavailable`/`model`，不会启动 provider 子进程。
15. `pollRemoteTasks` 对 provider health 为 `broken` 的 DeepSeek runtime 停止新 task claim，但不跳过前置运维操作队列，便于修复后恢复领取。
16. `runProviderTask` 在任务 claim 后再次检查 DeepSeek provider health；若 heartbeat 已将 runtime 标记为 `broken`，会在启动 `dsh` 前失败，避免健康状态竞态导致新执行。
17. 健康门禁不会把 `providerHealth.reason` 原样写入任务错误；对外仅返回固定的可行动文案，避免敏感 provider 输出进入审计链。
18. Web 创建入口只有在 `NEXT_PUBLIC_DEEPSEEK_HARNESS_RUNTIME_ENABLED=1` 时才开放 DeepSeek Harness；`0`、空值及其他字符串均保持关闭。
19. 频道聊天 composer 接受所有合法 `DaemonProvider` 绑定；DeepSeek Harness 不再被误判为无 runtime，权限菜单只显示 Runtime 默认，不暴露 Codex 专属完全访问选项。
20. 托管运行时列表服务统一解析 `metadataJson`/`lastError` 为 `RuntimeProviderHealth`；Web 列表和详情对 `broken` 显示“供应商不可用”并进入“需要处理”筛选，对 `degraded` 显示降级诊断但保持可用，对 `unknown` 显示待验证并不宣称已可调度；旧测试/历史 fixture 未提供健康快照时保持原展示兼容。
21. AI 员工执行引擎选项直接复用托管运行时已规范化的 provider health；只有缺失健康快照的历史 fixture 才使用兼容 fallback，DeepSeek broken 状态不会在员工绑定界面被重新显示为可用。
22. 状态优先级先判断 runtime offline，再判断 provider unusable/degraded/unknown；离线节点显示心跳诊断，在线但 DeepSeek provider broken 仍进入“需要处理”，避免两类故障混淆。
23. 新员工绑定路径现在同时受 UI 与服务端门禁保护：provider health `broken`/`unusable` 的 managed runtime 不可绑定，并显示 provider 原因；`unknown` 仍允许绑定以兼容首次探测和历史记录，但任务领取/执行健康门禁仍然生效。
24. 聊天模型覆盖和有效模型解析对 `deepseek-harness` 使用 runtime-local `deepseek-v4-flash`/`deepseek-v4-pro` 目录，不依赖 Models 服务是否已经返回 `deepseek_native`；解析结果仍标记为未验证，任务启动必须继续通过 provider health 的已验证 `modelIds` 门禁。
25. 托管任务 usage 以上下文 runtime provider 的主协议作为兜底：DeepSeek provider event、网关日志缺失 protocol 时仍记录 `deepseek_native`；网关明确返回的 protocol 优先。这样不依赖 provider 是否提供 gateway request id，也不会把原生 usage 误归类为 OpenAI 协议。
26. 员工绑定的执行引擎元信息对 `deepseek-harness` 明确显示 `Headless · no resume`，让 P0 的 fresh-session 边界在绑定时可见，不把 DeepSeek Harness 误认为支持现有 provider resume。
27. provisioning 的请求、凭证签发和 runtime ready 审计现在保留 provider、协议、默认模型和 allowlist 等非敏感元数据；DeepSeek 创建记录可以证明使用 `deepseek_native`，不记录明文 key。
28. 托管节点心跳检测到 managed runtime 的 provider health 状态跃迁时写入 `runtime.provider_health_changed` 审计，记录 status、error code、verification kind、protocol 和 model；原始 provider message 不进入审计数据，且审计写入失败不会阻断心跳。
29. provisioning 任务详情和 runtime 列表使用 `PublicRuntimeProviderHealth` 投影，仅公开 runtime/provider 状态、可用性、诊断原因、检查时间和结构化错误码；`lastProviderErrorMessage` 与 `rawProviderMessage` 不再进入公开服务记录。详情页对 DeepSeek broken/degraded/unknown 状态展示可操作的健康信息，与 runtime 列表使用同一状态语义。
30. `normalizeRuntimeProviderHealth` 对 reason、error message 和 raw provider message 做控制字符清理、凭据形态脱敏（API key/Bearer/token/secret/password）和 240 字符上限；即使旧 runtime metadata 已保存供应商原始文本，Web/员工诊断路径也不会直接显示凭据值。
31. 脱敏规则额外覆盖 JSON 引号字段（如 `"api_key":"..."`）和 URL query 参数（如 `?api_key=...`），避免 HTTP provider error 的常见结构绕过文本脱敏。
32. MCP Provider eligibility 已从 Web 展示条件收口为共享服务规则：只有对应实验开关严格为 `1` 的 Claude/Codex 可用；DeepSeek 在 connection materialization、任务 capability 投影、session claim 和 gateway 单次调用校验中均 fail-closed。升级前遗留的 DeepSeek `ready` 连接会降级为 `mcp.runtime_provider_not_eligible`，不会静默进入 adapter 不消费的 gateway 路径。
33. Provider 账户管理下拉复用 managed runtime 的可创建 Provider 列表；DeepSeek 仅在 `NEXT_PUBLIC_DEEPSEEK_HARNESS_RUNTIME_ENABLED=1` 时出现。`createProviderAccountAction` 与 `requestRuntimeProvisionAction` 同时执行服务端断言，不能通过直接调用 action 绕过 canary flag。
34. 上游 `dsh-v0.1.1-rc.2` JSON-RPC 契约与发布链已逐源码核验：生产 runtime 载体不在 `@deepseek-ai/dsh` npm CLI 包中，而由 `deepseek-harness-runtime-bin` wheel/单文件构建链产出；bundle 还包含相邻 `-rg`，macOS 包含 `-spawn-helper`。当前本机未构建或安装这些产物。wire 已提供版本协商、session cancel/resume/approval-policy 与 per-session endpoint overlay，仍缺 turn cancel ACK、逐请求 approval、session close 和跨进程 resume 证据。P2 已拆为“载体与同进程 session/stream”及“先扩展上游 wire 再做完整状态机”，当前不宣称达到 Codex 的 session/approval 水平。
35. AgentRouter 已实现显式双门禁的 one-shot JSON-RPC contract：固定 `0.0.1` server identity、root session owned receipt、连续 seq/time、turn/step 边界、七类 StreamChunk 的 block/usage/finish 状态、同 step 部分输出 retry 的失败 attempt 丢弃与状态重置、成功 attempt 文本 delta、durable tool、canonical usage、reasoning 输出与诊断隔离、1 MiB NDJSON 单帧上限、未知 required event 和失败后帧 fail-closed、capability 拒绝前资源释放，以及 ACK/无 ACK 两路的有界 EOF→SIGTERM→SIGKILL 退出。每任务仍创建新进程和随机 session，完成后 shutdown 且不发布 session id。
36. provider queue 已增加 operator release gate：只有严格开关、carrier/config 绝对路径，以及 carrier、config、`-rg` 和 macOS `-spawn-helper` 的精确 SHA-256 同时存在才选择 JSON-RPC；显式开启但平台所需配置不完整时 catalog 不注册、不回退 PATH、不执行 legacy probe。Cordis config 已从上游固定 tag 逐字纳入 `deploy/daemon/runtimes/deepseek-jsonrpc/cordis.yml`，release parser 与 spawn verifier 都要求其批准摘要 `048031be7331f2b68c81b3cbfacacc06ee767dae1e8f00cbdc3137c1e55001af`，操作员不能用任意自签名 config 绕过 composition 审查。adapter 在 spawn 前对 canonical bundle 文件做精确字节和 execute bit 校验，脚本 carrier 必须使用固定绝对解释器；task/Skill 的 `DSH_CORDIS_CONFIG`、PATH、动态加载与解释器启动变量无法改变 pinned carrier，release 控制变量不会进入 child。专用 `Dockerfile.deepseek-harness-runtime` 从命名 build context 复制 carrier/`-rg`/provenance，构建脚本先核对显式期望摘要，镜像层再次核对来源证明和批准 Cordis 摘要，烘焙 managed attestation marker，并将固定 tag、source commit、wheel/carrier/sidecar/config 摘要记录为 image labels。managed metadata 只有 marker 存在时才进入相同 verifier；缺 marker 的历史/异常镜像仍在 spawn 前拒绝。provider verification 在全部 pin 通过后运行有界 initialize/shutdown 握手并锁定 `0.0.1` server identity，无 credential/model canary 时标记 degraded。测试还证明错误 identity 与永不响应的 carrier 都会有界收口。代码与 fake runtime 证据已完成，但没有真实构建产物、不可变镜像或 endpoint E2E，不能据此开启生产。
37. 新增固定 Linux x64 wheel 导入链：`prepare-deepseek-runtime-bundle.py` 只接受 `deepseek-harness-runtime-bin==0.1.1rc2` 的 manylinux wheel，锁定 fork commit `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`，拒绝重复/越界/symlink ZIP member、错误发行元数据、额外 carrier、wheel 或 artifact 摘要不一致，并原子生成只读 executable 与确定性 `provenance.json`。`verify-deepseek-runtime-bundle.py` 被 staging preflight 与 Dockerfile 共用，伪造 source commit 在 Docker 调用前失败。真实 wheel 和独立发布摘要仍未取得。
38. managed provenance 已从构建证据提升为运行时准入：专用镜像传入 provenance 绝对路径、固定 source commit 和 wheel SHA-256；release parser 只在 managed marker 下要求这三项，standalone 行为保持不变。catalog、task 和 provider health 复用 verifier，严格验证 manifest schema、fork/ref/commit、wheel filename/distribution/version/tag、与 carrier 同目录，以及 manifest carrier/`-rg` 摘要和实际 pin 一致。测试证明 marker 存在但 provenance 缺失、source 被篡改、wheel 摘要冲突或 provenance 脱离 runtime 目录时均在 spawn 前拒绝；合法 attested manifest 才进入任务或有界 health 握手。该证据仍是 fake artifact contract，不等于真实镜像或 endpoint E2E。
39. 新增 `dofe-agent-daemon verify-deepseek-release` 与 tag 前镜像 smoke：命令要求完整 managed release 配置，复用实际文件/provenance verifier 和有界 initialize/shutdown health wire，只在成功后输出确定性的无路径、无 credential evidence。`verify-deepseek-runtime-evidence.py` 对来源、wheel/carrier/`-rg`/Cordis 摘要、server identity、NDJSON stdout purity 和 shutdown 结论做精确 JSON 校验。`build-managed-runtime-images.sh` 要求显式绝对 evidence 输出路径，镜像 build 后、tag 前执行命令，使用同目录临时文件校验后原子替换；无效结果不覆盖旧证据。当前测试使用 fake image evidence，因本机无真实 wheel、Docker daemon 和网络访问，尚未生成真实 release evidence。
40. 新增发布后原生双模型 canary 证据链：`dofe-agent-daemon verify-deepseek-model-canary` 只接受完整 managed release pin 与 `sha256:` 镜像摘要，在 release-only allowlist 环境中依次对 `deepseek-v4-flash`、`deepseek-v4-pro` 执行完整 one-shot JSON-RPC 回合，要求精确 canary 响应、无工具调用和唯一 canonical usage。`run-deepseek-runtime-canary.sh` 拒绝 mutable/option-like image ref，使用 `--pull=never` 并显式覆盖常驻 entrypoint，以唯一容器名、宿主 deadline、64 KiB evidence 上限和失败强制删除约束 Docker，只以环境变量名传递 `DEEPSEEK_API_KEY`；Python verifier 将模型顺序、状态、usage、server identity、30 分钟时效、官方/配置 endpoint 身份哈希、image digest 与 release evidence 精确绑定后原子替换旧证据。evidence 不含 prompt/output/key/明文 endpoint/主机路径。fake carrier 与 Docker runner shim 已验证双模型调用、其他 provider credential 隔离、key 不进入 Docker argv、entrypoint override、恶意 ref 零 Docker 调用、hang 有界清理、过期/无效结果失败保留旧证据；尚未用真实镜像和测试 key 执行。
41. model canary 已补齐 registry 签名准入：runner 要求不可变 image ref 的 repository 精确匹配独立批准值，并要求绝对、非 symlink、至多 16 KiB 的 PEM 公钥匹配独立批准 SHA-256。公钥与 release evidence 通过 `O_NOFOLLOW` 一次读取到 `0700` 私有目录；hash、scrubbed `cosign verify --key` 和最终 Python verifier 全部使用同一快照，消除路径替换窗口。DeepSeek key 与其他 provider credential 不进入 cosign，仓库/摘要不符、拒签、缺公钥或挂起均零 Docker 调用且保留旧证据。验签有默认 60 秒、范围 1..300 的独立宿主 deadline，超时按 SIGTERM→SIGKILL 有界收口；Docker client 也按 TERM→KILL 收口，唯一命名容器无论正常、非零或超时都执行有界强制删除。daemon evidence 的 `cosign-public-key` attestation 记录同一快照 SHA-256。fake cosign/Docker shim 已验证执行顺序、源 key 替换隔离、错误 repository/fingerprint、签名拒绝、挂起/非零清理和证据绑定；尚未取得真实已签名镜像、公钥与 registry E2E，因此仍为 Release blocked。
42. 新增 attested managed JSON-RPC 的 daemon queue lifecycle contract：测试从 `executeRemoteTask` 公共入口进入，使用完整 carrier/config/sidecar/provenance pin 执行 Flash 回合，生成 `runtime-output/agent-output.json` 和引用 artifact，验证 output bundle 上传、complete、provider usage 事件、Models gateway usage 合并及非会话 workdir cleanup。统一事件映射不再丢弃 DeepSeek 的 cache read/write/reasoning 字段；provider usage 只用于审计，增量 `reportTaskUsages` 与 completion `usages` 均只保留同一 gateway request id 和 `cacheTokens`，本地控制面 fake 按该 id 重放去重后仅形成一条结算记录。这是 fake carrier + 本地 control-plane client contract，不等于真实镜像/控制面 task queue E2E。
43. 闭合 post-push canary 与远端运行镜像的身份断点：新增受控 `deploy-deepseek-runtime.sh`，同一 shell 环境先运行 cosign 与 Flash/Pro canary，成功后才以 `--pull never --no-build` 启动专用 `docker-compose.deepseek-runtime.yml`。runner 要求完整 `DEEPSEEK_RUNTIME_IMAGE`、独立批准 repository 和独立 64 位 image SHA-256 三者完全一致；Compose 只按 `${repository}@sha256:${64hex}` 构造引用，且没有远端 `build:`、bundle build args 或 `MANAGED_RUNTIME_IMAGE_TAG` fallback。通用 `docker-compose.remote-images.yml` 不含 DeepSeek service，在未配置 DeepSeek 时仍可独立解析；本地 `docker-compose.runtimes.yml` 继续负责 bundle 构建。摘要冲突零 Docker、canary→deploy 顺序、两份远端 Compose 解析和静态契约测试已通过；尚未提供真实签名镜像，所以未执行远端启动。

## 4. 验证结果

| 验证 | 结果 |
| --- | --- |
| domain + provider template tests | 8 passed |
| DeepSeek daemon focused tests | 20 passed |
| DeepSeek model-catalog missing-model rejection | 1 passed |
| DeepSeek child cancellation/SIGTERM | 1 passed |
| AgentRouter 完整单文件回归（含 DeepSeek JSON-RPC/daemon release evidence CLI） | 42 passed |
| provider-runtime 完整单文件回归 | 62 passed；覆盖全部 provider、standalone JSON-RPC queue 准入与双模型 canary CLI |
| remote-daemon 完整单文件回归 | 40 passed；覆盖 DeepSeek headless/attested JSON-RPC queue lifecycle、artifact 回传、cache usage 审计、增量/completion gateway usage 一致性与幂等结算、cleanup |
| standalone/managed JSON-RPC release gate | provider-runtime 完整回归覆盖 carrier/config/sidecar 完整摘要、固定 tag composition 与固定解释器 spawn 前拒绝、operator config 优先、动态加载环境隔离、无副作用 catalog 探测、managed marker + provenance 严格准入，以及 pin 后有界 health wire 握手 |
| DeepSeek managed image contract | 22 passed；覆盖固定 wheel 导入、provenance、专用 Dockerfile/named bundle context、build 后/tag 前容器 smoke、cosign 公钥验签、不可变镜像双模型 canary、严格 evidence 校验和原子保留 |
| daemon dist smoke | 4 passed；CLI 与库入口打包后导出正常 |
| DeepSeek JSON-RPC 畸形事件/subagent fail-closed | 1 passed；覆盖 13 类负向帧，包括 usage 冲突、finish 后 chunk、turn 归属、未知 required event、超大无换行帧、reasoning 诊断隔离与失败后帧抑制 |
| AgentRouter subprocess 退出梯子 | 2 passed；覆盖 SIGTERM 与 5 秒后 SIGKILL |
| `pnpm --filter dofe-agent-daemon run types` | 通过；覆盖 domain/db/services/sandbox/daemon |
| Web DeepSeek feature-flag tests | 2 passed |
| Web chat composer regression suite | 29 passed |
| Web managed runtime provider-health regression suite | 6 passed |
| Web dashboard managed provider-health propagation | 2 passed |
| Web execution-engine broken provider binding guard | 1 passed |
| Managed runtime provisioning health mapping (`happy path`) | 1 passed |
| Services managed-runtime broken provider binding guard | 1 passed |
| Services DeepSeek native model resolution | 2 passed |
| DeepSeek usage protocol attribution (`remote-daemon.test.ts`) | 39 passed |
| `pnpm --filter @dofe-agent/services run types` | 通过 |
| `pnpm run typecheck:daemon` | 通过 |
| Web Vitest 全量（161 files / 1266 tests） | 通过 |
| DeepSeek provisioning audit metadata | 通过 |
| DeepSeek provider health transition audit (`routes.test.ts`) | 通过 |
| provisioning local-catalog preflight | 1 passed |
| Web managed runtime wizard | 5 passed |
| `pnpm run typecheck:deps` | 通过 |
| `pnpm run typecheck:web:only` | 通过 |
| provisioning 详情公开 health 投影（services Node test） | 通过；验证 raw provider message/error message 不出现在公开详情 |
| Web provisioning 详情 provider health 展示（task-detail-client.test.tsx） | 5 passed |
| provider health 文本脱敏（provisioning Node 单测 + runtime-health smoke） | 通过；Bearer/API key 值替换为 `[REDACTED]`，并验证敏感片段不再出现在公开详情 |
| JSON/URL provider health 凭据脱敏 smoke | 通过；带引号 `api_key` 和 query `api_key` 均替换为 `[REDACTED]` |
| MCP Provider 创建门禁 + 历史连接消费门禁 | 6 项聚焦测试通过；覆盖 DeepSeek materialization 拒绝、历史 ready 降级、claim 拒绝、gateway 拒绝和 capability completion |
| Provider 账户 DeepSeek canary 入口 | Web actions/panel/feature-flag 聚焦测试 21 项通过；相关 ESLint 通过 |
| 两份 Compose `config --quiet` | 通过 |
| deployment shell `bash -n` | 通过 |

本轮直接 daemon 测试运行于 Node `26.5.0`，pnpm 类型检查运行于 Node `25.9.0`；仓库 engines 要求 `^24.19.0`，pnpm/typecheck 命令产生 engine warning。该 warning 不影响上述聚焦断言结论，但发布节点必须回到 Node 24.19 基线后重跑。

## 5. 镜像构建阻断（已解除：切换到 Node 25.9）

执行 DeepSeek wrapper build 时，Docker 在拉取 `uhub.service.ucloud.cn/techwu/node:24.19-bookworm-slim` 阶段失败，registry 返回 `not found`。

2026-08-24 决策：批准基础镜像缺失，改为统一使用 `uhub.service.ucloud.cn/techwu/node:25.9-bookworm-slim` 作为所有 Node 容器基础镜像（含 `Dockerfile.deepseek-harness-runtime`、`Dockerfile.provider-runtime`、`Dockerfile.managed-node`、`Dockerfile`、`Dockerfile.mcp-egress-proxy`、`self-hosted`、`workflow-worker`、`feishu-worker`，以及 `deploy/daemon/.env.example` 与 `docker-compose.runtimes.yml` 的 `DEEPSEEK_HARNESS_PROVIDER_BASE_IMAGE` 默认值）。构建尚未进入 `npm install @deepseek-ai/dsh@0.1.1-rc.2`，真实镜像仍待用该 25.9 基础镜像构建并以 digest 锁定 `dofe/agent-runtime-deepseek-harness`。

## 6. 未执行项

- 未使用真实 `DEEPSEEK_API_KEY`，未调用真实 DeepSeek endpoint。
- 未创建真实 managed runtime，未执行 queue claim、runtime-output、artifact、cleanup E2E。
- 未在真实 endpoint 验证 Flash 最小任务或 Pro 编码任务；双模型完整回合 runner 与 fake carrier/image 契约已通过。
- 未在真实 DeepSeek endpoint 验证模型级 health；本地 mock 已覆盖完整目录和缺失模型拒绝。
- 未验证 headless 不监听端口、真实 SIGTERM 和容器内 env 隔离。
- 本地 fake child 已验证 AbortSignal -> SIGTERM -> `cancelled`；真实容器端口和退出取证仍未执行。
- 本地并发 fake child 已验证两个同目录 headless 调用分别读取 `deepseek-v4-flash`/`deepseek-v4-pro` overlay，且两个 UUID 文件均在进程退出后清理。
- 本地时间戳测试已验证启动新任务会清理 48 小时旧 overlay，同时保留近期 overlay。
- 本地并发 fake child 已验证两个 task 使用不同 `DSH_HOME`，且两个 task home 均在退出后清理。
- 本地 fake CLI 已验证 `--version` 成功但 headless help 失败时返回 `provider.profile_missing`/`profile`。
- 本地 fake control-plane smoke 已验证 DeepSeek task 生成的 runtime-output manifest/artifact 会上传并 complete，finally 后 task workdir 被删除；真实 queue claim E2E 仍未执行。
- 本地 provider test 已验证健康目录缺少所选模型时任务在 `dsh` 启动前失败，并返回 `provider.model_unavailable`/`model`。
- 本地 poll test 已验证 DeepSeek health 为 `broken` 时不调用 `claimTask`；运维操作队列仍保留独立处理路径。
- 本地 provider test 已验证任务 claim 后 health 转为 `broken` 时，`runProviderTask` 在 `dsh` 启动前返回 `provider.runtime_generic_failure`。
- 本地 provider test 已验证 health reason 中的敏感片段不会出现在门禁错误消息中。
- 本地 Web Vitest 已验证 DeepSeek 创建入口默认关闭，且仅严格值 `1` 开启。
- 本地 Web Vitest 已验证 DeepSeek 员工可使用聊天 composer，且权限菜单不会展示 Codex 的完全访问或审批策略。
- 本地 Web Vitest 已验证托管运行时列表在 provider health 为 broken/degraded 时展示诊断原因并进入 attention 筛选；provisioning happy path 已验证服务层输出 online/unknown/unverified 健康快照。
- 本地 Web Vitest 已验证员工执行引擎选项保留托管 DeepSeek provider health（broken/unusable/reason），不会在数据装配时丢失诊断。
- 本地 services Node test 已验证 provisioning 详情的 provider health 公开投影不包含原始 provider message 或完整错误消息；Web task detail 已验证健康状态、可用性和结构化错误码展示。
- provider health reason/error 的脱敏已在 normalization 层收口；真实节点仍应在 Node 24.19 镜像中复跑 health probe，确认 CLI/HTTP 错误格式变化不会产生新的未覆盖凭据形态。
- 本地 Web Vitest 与 services Node test 已验证 provider broken 时 UI 选项禁用、服务端 `bindEmployeeRuntimeSync` 返回 `runtime.provider_unavailable`；这只限制新增绑定，不改变既有绑定的取证和解绑路径。
- 本地 services Node test 已验证 Models catalog 为空时 DeepSeek Flash/Pro 覆盖与 runtime default 解析仍使用原生目录，且 `gpt-5` 等非原生模型被拒绝；这不替代真实 endpoint 的模型级 health/canary 验证。
- 完整 `runtime-provisioning.test.ts` 共 56 项，其中 55 项通过；唯一失败仍受现有测试数据库 schema 回放隔离影响（当前数据库版本高于测试 39->42 回放，报 `schema_version cannot be downgraded`）。本次 DeepSeek provisioning audit 用例已单独通过。
- 已补齐本地 usage contract 的 `deepseek_native` 协议归属，并新增 `run-deepseek-runtime-release-gates.sh`：真实路径要求 Models gateway 派生 endpoint、Models `RuntimeCredential` secret、签名双模型 canary、精确 digest 部署、真实 DeepSeek task 的 `STAGING_GATEWAY_REQUEST_ID`，随后通过现有 models 终态 billing reconciliation；当前仍未执行真实 wheel/image/endpoint/key、对账或生产部署。
- 已实现默认关闭的单任务 JSON-RPC contract，并用 fake runtime 验证文本/tool/usage 事件、owned receipt、durable 顺序、资源释放和退出梯子；provider queue 已实现 carrier/config/平台 sidecar 完整摘要、固定 tag composition、有界 health wire、固定 wheel provenance 导入，以及 managed image 装配/attestation 准入代码。上游 fork 已补齐 `session/cancel`、`session/resume`、`session/approval-policy` 和受控 `DEEPSEEK_BASE_URL` per-session overlay，TypeScript SDK 聚焦测试已通过；AgentSpace queue 尚未消费这些 wire。当前任务环境包含 Skill、credential attribution 与 gateway 日志路径，真实 wheel、签名 image、DeepSeek endpoint、billing 对账、bounded worker、同进程多轮和 session 映射仍未交付。
- DeepSeek MCP 当前为明确不支持而非“尚未显示”：服务端四层 fail-closed 已完成；只有未来 JSON-RPC Cordis composition 明确挂载并验证 MCP、且 approval/事件审计打通后，才可新增独立实验开关。
- 遵守工作站约束，未启动或触发 Jenkins。

### Wire 能力更新（2026-08-24）

上游 fork 已新增 `session/cancel`、`session/resume`、`session/approval-policy`、`session/close`、turn cancel ACK（`session.cancelled` 通知）、approval request/response（`approval.request` 通知 + `approval/respond` 请求）、受控 per-session `DEEPSEEK_BASE_URL` overlay 与 `protocolVersions`/`protocolVersion` 握手；AgentSpace one-shot 已发送并校验 `2.0`，queue 尚未消费 resume/approval-policy/close/cancel-ack/approval，仍仅在 abort 前 best-effort 发送 cancel。bounded worker 和真实恢复证据仍未完成。其中 `session/close` 在 fork 提交 `d847c90232` 落地（SDK server 28 项、client 49 项 vitest 全绿）；turn cancel ACK 与 approval request/response 在 fork 提交 `e0885d2d83` 落地（SDK server 31 项、client 49 项、protocol 19 项 vitest 全绿）。

## 7. 下一实施顺序

1. 修复/发布批准的 Node 24.19 基础镜像，构建 wrapper 并记录 image digest。
2. 在隔离测试环境运行 `dsh --version`、headless help、无 key、mock endpoint、SIGTERM 和端口检查。
3. 由 Models 服务确认 `deepseek_native` credential/gateway/usage contract；不要回退伪装成 OpenAI 协议。
4. 发布并以批准公钥签名镜像 digest 后，以测试 key 和该公钥运行 `run-deepseek-runtime-canary.sh` 取得 Flash/Pro evidence，再完成 workspace tool、runtime-output、cleanup 和 rollback canary。
5. 全部门禁通过后设置 Web 灰度开关。
6. P2a 从 fork CI 取得固定 wheel 及独立 wheel/carrier/sidecar 摘要，通过已交付导入器生成 provenance bundle，再用 named build context 和专用 Dockerfile 构建 managed image，记录不可变 image/in-image digest并验证 stdout purity 和真实 runtime E2E。queue 的完整 bundle、固定 composition、health wire 与 attested managed 准入代码已完成，不替代这些发布证据。
7. P2b 上游 fork 已增加并版本化 per-session environment、session cancel、approval-policy、resume、session close、turn cancel ACK、approval request/response 和 protocol negotiation wire；仍需实现 AgentSpace bounded connection manager、provider session 映射和同进程多轮；真实载体/端点/恢复证据到位前保持相关 capability false。
