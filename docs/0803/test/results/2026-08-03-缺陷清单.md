# 2026-08-03 缺陷清单

## 未关闭缺陷

| ID | 等级 | 标题 | 影响与证据 |
| --- | --- | --- | --- |
| QA-0803-001 | S1 | 核心 daemon 任务完成路径返回 409 | Web Vitest 中 6 条失败：legacy managed runtime、Runtime App input bundle、output bundle、飞书 manifest、direct-channel、mention_chat 均期望 200 实得 409，阻断任务/产物主链路准出 |
| QA-0803-002 | S1 | 静态质量门禁失败 | `pnpm run typecheck` 在 CLI 报 11 个 `QueuedTaskRecord` 缺少 `employeeId/employeeName`；`pnpm run lint:web` 报 2 个未使用变量错误 |
| QA-0803-003 | S2 | Runtime 移动端侧栏遮罩无法点击 | `390 x 844` 下 `.workspace-sidebar-overlay` 可见，但 `runtime-detail__field` 拦截 pointer events；Playwright 30 秒超时，见对应失败截图与 error context |
| QA-0803-004 | S2 | 直聊事件流反复中止，实时回复退化 | 真实消息提交后超过 45 秒未在探针等待窗口返回，期间 direct channel events 持续 `net::ERR_ABORTED`；稍后刷新确认已收到 `OK`，说明写入成功但实时链路/重连异常 |
| QA-0803-005 | S2 | 根全量测试命令参数传播错误 | `pnpm test -- --maxWorkers=2` 将参数传播进 services 的 shell `for` 循环，触发 `done --maxWorkers=2` 语法错误，无法按仓库约定完成根全量测试 |
| QA-0803-006 | S2 | 并发测试共享数据库不隔离 | Playwright 两 worker 及 Services/CLI 并发执行出现 PostgreSQL deadlock、schema migration lock 和数据冲突；单 worker/串行重跑可恢复 |
| QA-0803-007 | S2 | MCP 工具审计丢失 Actor ID | `mcp-tool-audits/route.test.ts` 期望 `actorId: agent-1`，实际传入 `undefined`，影响审计主体可追溯性 |
| QA-0803-008 | S2 | 数据保护健康告警测试无法执行 | `data-protection-health/route.test.ts` 未 mock `sendExternalPagerAlert`，错误级告警场景直接失败，Pager 行为无法由测试证明 |
| QA-0803-009 | S2 | Playwright 页面契约与当前 UI 不一致 | 移动技能文件编辑器、Runtime 标题和设置页 eyebrow 共 5 条断言失败；真实页面可打开，需确认产品回归或更新测试契约 |
| QA-0803-010 | S2 | SSO 启动失败会跳转到内部请求地址 | SSO 不可用时 `/api/auth/sso/start` 返回 `https://0.0.0.0:1455/auth/error?...`；`apps/web/app/api/auth/sso/start/route.ts:26` 使用 `request.url` 构造错误页，外部浏览器不可达 |
| QA-0803-011 | S3 | 登录成功后仍打印 cookie token 警告 | Console 出现 `No cookie token after login - SSO cookie may not be set`，但应用会话可用；会污染监控并掩盖真实认证问题 |
| QA-0803-012 | S3 | 测试输出存在 React act 与 sourcemap 警告 | 多个组件测试打印未包裹 `act(...)`；`@dofe/models-sdk` sourcemap 指向缺失源文件，降低失败信噪比 |

## 重点复现

### QA-0803-001

```bash
pnpm --filter @dofe-agent/web exec vitest run \
  app/api/daemon/routes.test.ts \
  app/api/cron/data-protection-health/route.test.ts \
  'app/api/daemon/tasks/[taskId]/mcp-tool-audits/route.test.ts' \
  --maxWorkers=2
```

当前结果：51 条中 43 通过、8 失败，其中 daemon routes 6 条返回 409。

### QA-0803-002

```bash
pnpm run typecheck
pnpm run lint:web
```

### QA-0803-003

```bash
pnpm --filter @dofe-agent/web exec playwright test \
  --config=../../docs/0803/test/results/playwright.runtime.config.ts \
  --workers=1
```

查看 `playwright-artifacts/workspace-navigation-keeps-b9a41-e-in-a-constrained-viewport/`。

## 已验证关闭/被后续提交覆盖

MCP egress proxy 在早期工作树曾出现 lease 校验失败和测试挂起。末次完整复核 HEAD `241a3ad` 重跑 44/44 通过，因此不列为当前未关闭缺陷；候选 commit 冻结后仍应纳入回归。
