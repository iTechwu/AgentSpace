# AgentSpace 全系统测试报告

## 1. 结论

**本轮修复及可执行回归通过。** 最新产品基线 `87d305c` 已通过全仓类型检查、Web Lint、受限并发的 8 包根全量测试、Web 752 条用例与 Chrome Playwright 16 条用例。服务重启后本地/域名健康接口、SSO discovery 和 SSO 启动跳转均正常。

160 条业务矩阵中 15 条已按步骤关闭且全部通过，145 条仍因多角色、飞书/MCP、异常 Runtime、备份恢复、多浏览器等前置缺失而阻塞。因此可判定“本轮修复验证成功”，但不将未执行场景虚报为 160/160 发布准出。

## 2. 测试基线

| 项目 | 结果 |
| --- | --- |
| 测试时间 | 2026-08-03，Asia/Shanghai |
| Web 地址 | `https://agentspace.local.dofe.ai` |
| 产品修复 HEAD | `87d305cd56d49b68a1fb2223d7b037c67294d8d1` |
| 分支 | `techwu/feature-mcp-skill-data-durability` |
| 浏览器 | Google Chrome `150.0.7871.187`，桌面 `1440 x 960`，移动 `390 x 844` |
| 账号 | 测试管理员，手机号后四位 `8000` |
| 工作区 | “优惠豚 / 全体”，slug `yootun-all-优惠豚-全体-87e967` |
| Runtime | remote；1 个在线节点、2 个可用执行引擎 |
| 数据库隔离 | 全量自动化使用独立 PostgreSQL schema，结束后自动删除 |
| 浏览器工具 | 无 Chrome DevTools MCP；按技能规程使用系统 Chrome + Playwright 隔离上下文 |

## 3. 用户与服务结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 首页与 SSO 登录 | 通过 | SSO 恢复后管理员登录成功，默认工作区正确 |
| 工作区与 23 个模块 | 通过（冒烟） | 均可打开，有对应内容，桌面端未见横向溢出 |
| 平台运维/审计 | 通过 | 管理员可访问且显示管理内容 |
| 真实 Agent 私聊 | 通过 | 向 `Claude E2E` 发送探针，消息回显并最终收到 `OK` |
| 移动导航 | 通过 | `390 x 844` 下侧栏遮罩、模块跳转、技能编辑和 Runtime 页 E2E 通过 |
| 服务重启 | 通过 | Next.js 16.2.12 在 1455 端口 Ready，本地与 HTTPS 健康接口均 200 |
| SSO 健康 | 通过 | discovery 200；`/api/auth/sso/start` 为 307 且目标为 `sso.ixicai.cn` |
| 凭据泄漏 | 通过 | URL、结果 JSON、截图与文档未记录密码、Cookie、Token 或 Secret |

页面导航旧样本为 23 个模块平均 `2.78s`、P50 `2.83s`、P95 `3.71s`、最大 `4.02s`。项目尚未给出可量化性能预算，该数据仅作基线。

## 4. 自动化结果

| 检查 | 结果 | 摘要 |
| --- | --- | --- |
| `pnpm run typecheck` | 通过 | 全仓 0 错误 |
| `pnpm run lint:web` | 通过 | ESLint `--max-warnings=0` |
| `pnpm test -- --maxWorkers=2` | 通过 | Turbo `8/8`，`--concurrency=2`，耗时 `3m38s` |
| Web Vitest | 通过 | 103/103 files，752/752 tests |
| Playwright Chrome | 通过 | 16/16，1 worker，独立 schema；失败列表为空 |
| CLI | 通过 | 20/20 |
| Daemon | 通过（条件跳过） | 139 通过，3 条真实 Docker 条件用例跳过 |
| MCP egress proxy | 通过 | 48/48 |
| Domain / Sandbox / DB | 通过 | 18/18、13/13、16/16 |
| PostgreSQL 新 schema | 通过 | 512/512 statements；非 `public` schema 下 105 张表 |
| DB 配置/隔离定向 | 通过 | 18/18 |

根全量不再出现 PostgreSQL deadlock、跨 schema 读写或 services/CLI 数据冲突。React `act(...)` 和 Node experimental localStorage 告警已清理；`@dofe/models-sdk@0.5.5` 缺失源 sourcemap 是上游发布包元数据问题，不影响应用行为。

## 5. 修复摘要

1. 补齐 daemon/CLI 任务员工身份契约，修复任务完成写租约、技能门禁与 MCP 审计 Actor 传递。
2. 修复 SSO 错误回跳外部 Origin，收敛直聊 SSE 重连，避免不必要连接和重复刷新。
3. 修复 PostgreSQL 非 `public` schema 初始化、重复列声明和测试库覆写，以独立 schema 消除迁移锁竞争。
4. 稳定 Playwright Cookie 范围、移动技能编辑、Runtime/设置页面契约与 Web 异步测试。

## 6. 阻塞与证据

剩余 145 条只因环境/数据前置阻塞：多角色与第二工作区、完整 CRUD 数据集、异常 Runtime/Skill/MCP、飞书绑定/测试群，以及 Edge、Safari、读屏、慢网、恢复、长稳和 Docker 节点。本轮未留下可复现的 S1/S2 产品缺陷。

- 真实用户脱敏截图：[evidence](./evidence/)
- 最终 Playwright JSON：[playwright-results.json](./playwright-results.json)；`.last-run.json` 为 `passed`，`failedTests` 为空。
- 旧失败截图和 error context 已清理；trace 因可能包含会话元数据不予保留。
- 本轮未 push、未部署，且未启动或触发 Jenkins。

## 7. 准出建议

当前修复可作为独立交付。若需要严格的全量发布签字，应先补齐第 6 节前置、冻结候选 commit，再执行剩余 145 条矩阵。
