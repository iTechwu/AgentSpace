# 07 验收审查与差距

> 审查基线：`1cf73255`；审查对象：0815 优化提交 `7f360d27..585ab010`（SkillRolloutPlan 依赖闭包 + 多 Runtime 批量安装 + novel-production 编排）。
> 结论：**当前实现为「部分完成」，不建议直接作为 0815 的完整验收版本。** 基础类型、闭包解析骨架、模板 DAG 与原子 upsert 已落地，但审批安全边界、Runtime placement、幂等约束、dispatch preflight 与产物契约仍未达到文档验收标准。
>
> 本文档对照 [04-实施计划与验收](./04-实施计划与验收.md) 的 ACC-01~10 与 [05-测试矩阵](./05-SkillRolloutPlan落点与测试矩阵.md) 的 T01~T22，记录实现与验收之间的差距。审查发现经逐项复核（见文末）。

---

## 一、Spec 差距（按优先级）

### P1（阻断验收）

| # | 问题 | 位置 | 对应验收项 |
| --- | --- | --- | --- |
| 1 | **安装项完全忽略 placement，Runtime 映射错误**：对所有目标 Runtime 生成 root + 全部 closure 的笛卡尔积，不区分 `same_runtime` 与 `workflow`；workflow 依赖被过度安装或被错误要求共置；`all-compatible` 把不兼容 Runtime 全部纳入 | `rollout.ts:222-237`、`rollout.ts:212`；`validation.ts:239-245` | ACC-03（目标集合）、决策摘要 #1、风险 #4（Runtime 能力差异） |
| 2 | **Rollout 审批可被重放，缺 planDigest 校验**：只校验 plan 存在/批准/artifact/runtime 在范围内，输入无 planDigest、不检查 consumedAt；`consumeSkillRolloutPlanSync` 存在但未接入安装流程，已消费的批准计划仍可重复创建子安装 | `installations.ts:145-152` | ACC-04（一次审批）、决策摘要 #5、风险 #3（审批越界）、T17–T19 |
| 3 | **skill_rollout_plan 未实现幂等唯一约束**：只有普通索引，缺 `UNIQUE(workspace_id, plan_digest)`；测试明确断言相同 digest 创建新计划 | `postgres-schema.ts:3547-3568`、`skill-rollout-plans.test.ts:53` | ACC-05（原子幂等）、04 文档 DDL、T14 |
| 4 | **运行时 dispatch 未执行闭包 ready 门禁**：`validateWorkflowNodeForDispatchSync` 构造 inventory 时未传 `workspaceId`/`runtimeBindings`，闭包校验直接返回空结果；发布路径可能检查，运行/dispatch 路径仍可绕过 | `validation.ts:187`、`validation.ts:225` | ACC-06（preflight 门禁）、04 文档 P4 阶段 |
| 5 | **Artifact Revision 与 quality-report.json 契约未落地**：文档要求 artifactId/revision/digest/producedBy/inputRefs/schemaVersion 并统一记录质量门结果；当前模板仅声明 artifactDigest/revision/blockingCount 等输出字段，无 manifest/schema 校验或持久化契约 | `novel-production-template.ts:40` | 04 文档 P0 阶段、02 文档 Artifact Revision 契约 |

### P2（可接受但不满足文档）

| # | 问题 | 位置 | 对应验收项 |
| --- | --- | --- | --- |
| 6 | **可选依赖被静默丢弃**：required-only 模式直接跳过可选依赖，plan 中无 skipped 状态，无法解释该依赖为何未安装 | `rollout.ts:143-147` | 风险 #2（可选依赖 skipped 标记） |
| 7 | **依赖冲突被静默去重**：只按 coordinate 保留第一条声明，不检测同 coordinate 的版本范围或 placement 冲突 | `dependencies.ts:134-141` | ACC-01（循环/冲突检测） |
| 8 | **SkillRolloutPlan 接口缺 planId**：返回结构只有 planDigest 无 planId，planner 结果无法直接关联持久化审批记录 | `rollout.ts:44-53` | 决策摘要 #5 |

---

## 二、Standards 问题

| # | 类别 | 位置 | 说明 |
| --- | --- | --- | --- |
| S1 | 硬性风格 | `validation.ts:146` | `const blocker` 缩进错误 |
| S2 | 未使用导入 | `rollout.ts:10` | `SkillArtifactRecord` 未使用 |
| S3 | 注释错位 | `import.ts:631` | artifact 持久化注释贴到 `deriveSkillCoordinate`，真正的 `prepareSkillArtifact` 缺说明 |
| S4 | 判断性气味 | `dependencies.ts` / `novel-production-entry.ts` / `novel-production-template.ts` | 依赖 frontmatter 解析逻辑重复；`novel-production-entry.ts` 重复维护六个 coordinate；production helper 命名含糊 |

---

## 三、验证结果

| 项 | 结果 |
| --- | --- |
| `@dofe-agent/services` 类型检查 | ✅ 通过 |
| `@dofe-agent/db` 类型检查 | ✅ 通过 |
| rollout / dependencies / workflow template 相关测试 | ✅ 通过 |
| installations 测试 | ⚠️ 27/30，3 个因 PostgreSQL 后台维护锁忙与 deadlock 失败，未形成完整通过结论 |
| `git diff --check` | ✅ 通过 |

---

## 四、结论与建议

1. **不完整验收**：P1-1~P1-5 五处均命中验收清单的硬性要求（目标集合、一次审批、幂等、preflight、产物契约），需修复后才能作为 0815 完整验收版本。
2. **优先修复顺序**（按风险与依赖）：
   - P1-3（幂等唯一约束）→ P1-2（planDigest/consumedAt 防重放）→ P1-1（placement 正确映射）→ P1-4（dispatch preflight）→ P1-5（Artifact Revision 契约）。
   - P1-3/P1-2 是审批安全边界，属 fail-closed 性质，应最先处理。
3. **P2 与 Standards** 可随上述修复一并收敛，或单列 PR。

---

## 五、复核说明

本次整理前对审查报告的关键发现做了逐项核对（读源码对应行）：

- P1-1 笛卡尔积：`buildSkillRolloutItemsSync` 确为 `runtimeIds × digests`（root + 全部 closure）双重循环，无 placement 区分。
- P1-2 缺 planDigest/consumedAt：`installations.ts:145-152` 仅 `readSkillRolloutPlanSync` + `assertRolloutPlanCoversInstallationSync`，无 planDigest 入参、无 consumedAt 检查。
- P1-3 缺 UNIQUE：`skill_rollout_plan` 仅两个普通索引，无 `UNIQUE(workspace_id, plan_digest)`。
- P2-7 冲突去重：`uniqueSkillDependencies` 仅按 `coordinate.toLowerCase()` 去重保留首条，无版本/placement 冲突检测。
- P2-8 缺 planId：`SkillRolloutPlan` 接口仅 `planDigest`，无 `planId`。
- S2 未使用导入：`rollout.ts:10` 的 `import type { SkillArtifactRecord }` 未见使用。

其余发现（P1-4/P1-5/P2-6/S1/S3/S4）为审查报告原始结论，未逐一复核，但与前文已核实项同源、可信度高。
