# AI 员工团队编排：后端设计文档

> 实施边界（2026-08-06）：本文件的 Server Actions、内部事件入口和事件分页是当前接口事实；下方通用 REST 表仅作为未来外部 API 设计，不代表已提供。`root_task_id`、独立 attempt 表、节点超时和实际成本熔断是预留/二期能力。

## 1. 服务与仓储边界

```text
packages/domain/src/workflows.ts
  WorkflowDefinition / Version / Node / Edge / Trigger / Run / NodeRun / Event

packages/db/src/workflows/
  definitions.ts  runs.ts  events.ts  outbox.ts

packages/services/src/workflows/
  definitions.ts  validation.ts  publishing.ts  scheduler.ts  coordinator.ts
  dispatcher.ts  inputs.ts  retries.ts  approvals.ts  events.ts  recovery.ts
  materialization.ts  migration.ts  feature-flags.ts  observability.ts  security.ts

apps/workflow-worker/
  src/index.ts  src/worker.ts
```

`packages/db` 只实现事务和行映射；`packages/services` 持有状态机、权限、图规则和幂等语义；Worker 只循环调用 service，不重复业务规则。Web 和 daemon 不直接修改 Workflow 表。

## 2. 数据模型

### `workflow_definition`

`id, workspace_id, name, description, owner_user_id, channel_name, status, active_version_id, draft_graph_json, draft_version, legacy_source_type, legacy_source_id, created_by, created_at, updated_at, archived_at`

约束：`UNIQUE(workspace_id, id)`；legacy source 在 workspace 内唯一；status 为 `draft|published|paused|archived`。

### `workflow_version`

`id, workspace_id, workflow_id, version_number, schema_version, graph_json, input_schema_json, output_schema_json, governance_json, content_hash, published_by, published_at, created_at`

发布后不可更新。`UNIQUE(workflow_id, version_number)` 和 `UNIQUE(workflow_id, content_hash)` 避免重复版本。

### `workflow_trigger`

`id, workspace_id, workflow_id, type, config_json, timezone, status, next_fire_at, last_fire_at, misfire_policy, dedupe_window_seconds, lease_owner, lease_expires_at, created_at, updated_at`

`type` 首期为 `manual|schedule|event`。Schedule config 保存受控 repeat 或验证后的 cron，不保存本地化日期字符串。

### `workflow_run`

`id, workspace_id, workflow_id, version_id, root_task_id, trigger_id, trigger_type, trigger_key, input_json, status, current_sequence, budget_json, started_at, finished_at, created_by, created_at, updated_at`。其中 `root_task_id` 为兼容投影预留，首期不保证物化。

`UNIQUE(workspace_id, trigger_key)` 保证同一计划时间/事件只创建一个 Run。

### `workflow_node_run`

`id, workspace_id, run_id, node_id, node_type, employee_id, employee_name_snapshot, status, attempt_count, max_attempts, available_at, task_queue_id, approval_id, input_json, output_json, artifact_manifest_json, error_code, error_message, started_at, finished_at, created_at, updated_at`

`UNIQUE(run_id, node_id)`；首期在 Node Run 上记录递增 `attempt_count` 并关联每次生成的队列任务，独立 `workflow_node_attempt` 审计表为二期增强。

### `workflow_run_event` 与 `workflow_outbox`

事件表：`id, workspace_id, run_id, node_run_id, sequence, type, actor_type, actor_id, severity, data_json, created_at`，唯一 `(run_id, sequence)`。

Outbox：`id, workspace_id, aggregate_type, aggregate_id, event_type, payload_json, status, attempts, available_at, locked_at, locked_by, last_error, created_at, published_at`。

索引至少覆盖：到期 Trigger、ready/retry_wait Node Run、运行列表、Run 事件顺序、未发布 outbox 和 task_queue 反查。

### `agent_task_queue` 与 `task_commit_journal`

Workflow 员工节点复用 `agent_task_queue`，队列状态扩展为 `queued|claimed|running|preparing_commit|committed|completed|failed|cancelled`。AI 输出提及任务可提供 workspace 范围的幂等键，队列 ID 由该键稳定派生，冲突时返回已有任务且不重复写生命周期事件。

`task_commit_journal` 记录 `task_id, workspace_id, employee_id/name, workspace_revision_id, artifact_ids_json, commit_state, attempt, error_code, error_message, timestamps`。状态更新未提供 revision/artifact 时必须保留已有值；普通状态检查点不增加 attempt，只有实际对账失败才增加。

## 3. Graph JSON 契约

```json
{
  "nodes": [
    {"id":"research","type":"employee_task","employeeId":"emp_1","config":{"instruction":"...","outputFields":["report"],"retry":{"maxAttempts":2}}},
    {"id":"analysis","type":"employee_task","employeeId":"emp_2","config":{"input":{"report":"${nodes.research.output.report}"}}},
    {"id":"audit","type":"employee_task","employeeId":"emp_3","config":{"input":{"report":"${nodes.research.output.report}"}}},
    {"id":"join","type":"join","config":{"policy":"all_success"}},
    {"id":"editor","type":"employee_task","employeeId":"emp_4","config":{"input":{"results":"${join.outputs}"}}}
  ],
  "edges": [
    {"source":"research","target":"analysis"},
    {"source":"research","target":"audit"},
    {"source":"analysis","target":"join"},
    {"source":"audit","target":"join"},
    {"source":"join","target":"editor"}
  ]
}
```

表达式首期不是通用脚本，只支持声明式 `${run.input.*}`、`${nodes.<id>.output.<field>}` 与 `${join.outputs}`。员工节点通过 `outputFields` 声明最多 32 个唯一字段；字段名必须匹配 `[A-Za-z][A-Za-z0-9_]{0,63}`，下游只能引用已声明的上游字段。Daemon 将解析后的节点输入加入任务上下文；非默认 `text` 输出必须返回精确 JSON 对象。完成适配器只读取自有属性、丢弃未声明字段，并拒绝缺字段或超过 256 KiB 的输出。运行时不执行 `eval`、shell 或用户 JavaScript。

## 4. 核心算法

### 发布

1. 规范化节点和边，验证 schema version。
2. 拓扑排序检测环、孤立节点和不可达路径。
3. 校验 Join 的上游、员工稳定 ID、Runtime/Skill readiness、频道成员关系、节点/工作流预算和权限。
4. 计算 `content_hash`，在同一事务中插入不可变 Version、更新 Definition active version，并按需 upsert Trigger；重复内容发布仍允许只更新 Trigger。
5. 写 `workflow.version_published` 审计和 outbox。

Definition 创建与草稿更新本身也必须与 `workflow.definition.created|updated` 审计在同一数据库事务提交；更新 actor 只能来自鉴权上下文，审计仅记录 `changedFields` 字段名数组而不记录字段值。`retry.maxAttempts` 即使绕过 UI 直接调用服务端，也必须为 1-10 的整数，非法值返回稳定码 `workflow_retry_policy_invalid`。

### Scheduler Tick

```text
claim where status=active and next_fire_at<=now and lease expired
for each trigger:
  derive triggerKey(workflowId, triggerId, scheduledAt/eventId)
  create workflow_run with ON CONFLICT DO NOTHING
  materialize node runs from immutable version
  advance ready nodes
  compute next_fire_at
  persist next_fire_at + run.created + trigger.fired + ready outbox in same transaction
```

确定性配置错误暂停 Trigger；瞬时物化错误保留租约等待重试。`skip` 与 `fire_once` 的结果写稳定审计码，`fire_once` 审计与 Trigger 推进同事务提交。Worker 与 reconcile API 返回 `schedulerFailures`，便于告警与值班定位。

### 图推进

收到节点终态事件后锁定 Run，验证事件 task/node 归属与 attempt。对每个下游节点计算：所有 required 前置是否终态、Join policy 是否满足、Run 是否暂停/取消。满足则条件更新 `pending → ready` 并写 outbox；最终失败按普通后继或 Join policy 传播为 failed/skipped。重复事件不得创建第二条队列任务。实际成本预算的运行中判定为二期。

### Dispatch

读取 ready outbox，先在数据库事务中原子 claim Node Run，并按 Version governance 统计同一 Run 的 queued/running 数；超限进入短暂 `retry_wait`。随后再次复核员工、Runtime、Skill 和频道 readiness，解析声明式输入与 Artifact refs，再写入现有任务队列。若队列写入失败，Node Run 回到 `retry_wait`，不把 Workflow 标成成功。

### 完成提交与对账

1. 锁定 Workflow Run 与队列任务，拒绝终态迟到回调；`paused` 只阻止新 dispatch，已领取任务仍可取输入并跨过 start。
2. 将输出 bundle 物化到任务 staging；一旦副作用快照存在，后续重试不得用新请求体覆盖 staging。
3. 进入 `preparing_commit` 后应用受控副作用并原子写 `.completion-effects.json`。检查点前发生部分副作用则进入人工补偿，不自动重试。
4. 按领取时的 binding generation 提升 Workspace 输出，写 revision/artifact 后进入 `committed`。
5. 在同一数据库事务中把队列任务、Workflow Node Run 转为成功，并把 journal 改为 `projection pending`；随后由共享 `projectTaskCompletion` 适配器幂等投影任务看板、频道消息、飞书 outbox、会话状态和自动续接，HTTP 首次完成与 Cron 恢复不再各自维护一套投影逻辑。本地 Daemon 遵循相同约束：频道消息已存在时仍继续补偿 outbox，回复按消息/分片稳定键、审批状态卡按消息/状态/任务/approval scope 幂等落库；任何 outbox 入队异常都让 journal 保持 pending，不能提前标记 committed。
6. 投影全部完成后 journal 回到 `committed` 并清理 staging。HTTP 重试立即重驱投影；恢复 Cron 还会扫描全部 workspace 的 stale `preparing` 及“任务仍为 committed”的日志。

本地 Daemon 也写兼容的 staging/副作用快照。恢复扫描先以条件更新确认心跳确实过期，再回收 Runtime 与 Daemon 均离线的 `claimed/running` 任务；队列失败和 Workflow 重试决定在事务内完成。

## 5. API 与 Action 契约

当前实际接口：Web mutation 使用 `workflow-actions.ts` 中的 Server Actions；运行事件使用 `GET /api/workspaces/:workspaceId/workflow-runs/:runId/events?after=`；平台事件使用受 `CRON_SECRET` 保护的 `POST /api/internal/workflows/events`；图调度恢复使用 `/api/cron/workflows/reconcile`，提交日志恢复使用受同一密钥保护的 `/api/cron/task-commit-reconcile`。

以下是未来 CLI/外部集成可采用的 HTTP 契约，不属于首期已实现接口：

| Method | Path | 语义 |
| --- | --- | --- |
| GET/POST | `/api/workspaces/:workspaceId/workflows` | 列表/创建草稿 |
| GET/PATCH | `/api/workspaces/:workspaceId/workflows/:workflowId` | 读取/更新草稿 |
| POST | `.../:workflowId/validate` | 图和依赖预检 |
| POST | `.../:workflowId/publish` | 发布不可变版本 |
| POST | `.../:workflowId/runs` | 手动触发 Run |
| GET | `.../:workflowId/runs` | 运行历史 |
| GET | `/api/workspaces/:workspaceId/workflow-runs/:runId` | 运行 projection |
| GET | `.../:runId/events?after=` | 事件补偿/SSE |
| POST | `.../:runId/pause|resume|cancel` | 运行控制 |
| POST | `.../:runId/nodes/:nodeId/retry` | 节点重试 |

未来 HTTP API 应复用现有 service，并为创建 Run 等可重放 mutation 接受 `idempotencyKey`，为草稿更新接受 `expectedVersion`；当前 Server Actions 已通过 workspace context、draft version、trigger key 和服务端唯一约束实现对应冲突保护。手动物化还会锁定 Definition 并读取其权威 Trigger，只有 `published + active manual` 组合可创建 Run，不得通过直调 Action 为 schedule/event Workflow 另建 manual Trigger。

## 6. 事件契约

首期当前内部事件（尚未承诺为外部公共协议）：

- `trigger.fired`
- `run.created|paused|resumed|cancelled`
- `node.started|succeeded|failed|skipped|retry_scheduled|dependency_blocked|concurrency_wait`
- `join.succeeded|failed`
- `approval.requested|approved|rejected`

Run/Node 的权威终态同时存在事实表中，UI 不依赖某个事件名称推断状态。Payload 只携带稳定 ID、状态码、时间、计数、Artifact 引用和安全参数；不携带秘密、未经脱敏日志或可被 UI 直接渲染的外部 HTML。公共事件版本化在提供外部订阅前另行定义。

## 7. 错误码

| 错误码 | HTTP/状态 | 处理 |
| --- | --- | --- |
| `workflow_graph_cycle` | 422 | 阻止发布并返回涉及节点 |
| `workflow_node_unreachable` | 422 | 定位孤立节点 |
| `workflow_employee_not_ready` | 409 | 返回员工 readiness blocker |
| `workflow_version_conflict` | 409 | 返回最新版本/更新时间 |
| `workflow_trigger_duplicate` | 200 existing | 返回已有 Run，不重复执行 |
| `workflow_budget_exceeded` | 422/发布阻塞 | 首期降低预计成本、并发或重试；运行中实际成本熔断为二期 |
| `workflow_node_retry_exhausted` | failed | 保持失败终态并通知；具备控制权限的用户可追加人工重试，系统扩展该节点的尝试上限并重算后代 |
| `workflow_retry_policy_invalid` | 422/发布阻塞 | `maxAttempts` 不是 1-10 的整数；修正节点重试策略后重新预检 |
| `workflow_output_invalid` | failed/retry | Agent 未返回节点声明的结构化字段；保留稳定错误码并按节点策略重试 |
| `workflow_output_too_large` | failed/retry | 结构化输出超过 256 KiB；改用 Artifact 引用或缩小摘要 |
| `workflow_completion_effect_uncertain` | failed/manual | 外部副作用可能只完成一部分；禁止自动/人工直接重试，先核对并补偿 |
| `workflow_runtime_offline` | failed/retry | Daemon 与 Runtime 心跳均离线；按节点重试策略生成新 attempt |
| `workflow_task_setup_failed` | failed/retry | 本地执行准备失败；队列与 Workflow 原子失败并清理非会话工作目录 |
| `workflow_event_sequence_gap` | reconciling | 暂停投影并补偿 |
| `workflow_cross_workspace_reference` | 403 | 审计并拒绝 |
| `workflow_workspace_mismatch` | 409 | Definition、Version、Trigger、Run、Node Run 或根任务归属不一致时拒绝 |
| `workflow_node_type_unsupported` | 422 | 拒绝首期白名单之外的节点类型 |

## 8. 权限、审计与数据保留

复用 workspace role 与 actor context。Definition/Version/Trigger 的变更审计记录 diff 摘要，不记录 secret；Run/Node/Attempt 的每次控制动作写 actor 和 reason。删除 Workflow 采用归档，不级联删除 Run、Artifact、approval 和 audit。数据清理按现有 workspace purge 和员工数据保留策略扩展，Legal Hold 下禁止清理关联运行证据。

## 9. 兼容与迁移实现

- 为 legacy `ScheduledTask.id` 和 `AutomationRule.id` 建 `legacy_source_id` 唯一映射表或列。
- 迁移脚本支持 `--dry-run`、workspace 范围、统计和幂等重跑；每条失败保留原因。
- 切流按 workspace feature flag：`legacy_only → dual_read → workflow_engine → legacy_archived`。
- 双写期间只有一个调度 owner；旧 ScheduledTask 的 UI 写操作转发到新 service，避免双触发。
- 内置“然后”和“做直到”保持 legacy adapter，直到新 Engine 支持等价运行语义并完成历史兼容测试。
