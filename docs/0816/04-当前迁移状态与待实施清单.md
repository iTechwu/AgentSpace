# 当前迁移状态与待实施清单

审查基线：`dev` 分支 `9f124dec` 之后的当前工作树，2026-08-17。

## 1. 当前迁移状态

| 优先级 | 项目 | 状态 | 审查证据 |
| --- | --- | --- | --- |
| P0 | PostgreSQL schema 应用 | **阻断** | 代码 `POSTGRES_SCHEMA_VERSION=121`，当前 dev 数据库 `schema_version=117` |
| P0 | rollout reconcile schema | **代码完成、数据库未完整应用** | 表能力已有测试；`postgres:verify:invariants` 实际发现缺少 `idx_skill_rollout_reconcile_plan_status` |
| P1 | Prisma contract drift | **部分完成** | 已覆盖列、默认值、PK、unique/index、FK/onDelete |
| P1 | PostgreSQL invariant drift | **本轮新增基础 gate** | 从 schema source 提取 index/function/trigger/命名 CHECK，并与 catalog 比对 |
| P1 | Prisma 切流 flag | **基础门禁完成** | 23 个域集中注册，未知/非法/不一致 flag fail-closed |
| P1 | Prisma 连接容量 | **配置完成、运行证据缺失** | role pool/timeout/application_name 有单测；缺少 Web/worker/daemon 压测与 waiting/timeout 指标 |
| P2 | shadow SLO / 回滚 | **进程内聚合完成、生产闭环待实施** | 有界窗口已计算域级 mismatch/fallback/error/P95 和 rollback reasons；缺少持久化、跨实例聚合、告警与发布系统执行 |
| P2 | 剩余写路径 | **待实施** | task enqueue、outbox、workflow lease 等尚无统一 caller/事务/幂等/事件顺序 inventory |

## 2. 本轮已实施

1. 新增 `postgres:verify:invariants`，并接入 DB `pretest`。
2. 新增 invariant contract 提取与比较测试，覆盖 SQL 注释过滤和缺失对象诊断。
3. 对当前 dev 数据库执行门禁，得到真实失败证据：缺少 reconcile 状态索引。
4. 新增 `PrismaCutoverSloWindow`，在日志采样前记录完整样本，按域聚合 mismatch/fallback/error/P95。
5. 新增阈值评估、rollback reasons、flag version 与 last-known-good version 快照；相关测试 16 项通过。

## 3. 待实施顺序

1. **P0：应用 117 -> 121 schema 迁移。** 必须走项目允许的迁移/部署流程；本工作站不启动或触发 Jenkins。迁移后重新执行 `prisma:verify:contract` 和 `postgres:verify:invariants`。
2. **P1：扩展 invariant gate。** 比较 CHECK 表达式、partial index predicate、enum label、view definition 和 function body hash，而不只检查对象名称。
3. **P1：建立连接池容量证据。** 分别记录 Web、worker、daemon 的 active/waiting/timeout、P95 和 graceful shutdown。
4. **P1：持久化 cutover SLO。** 将现有进程内快照写入集中指标，补充 deadlock/P2034、跨实例 burn-rate、告警和发布系统回滚演练。
5. **P2：建立剩余写路径 inventory。** 每条路径记录调用方、事务边界、幂等键、affected rows、事件副作用和退出 legacy 的验收条件。

## 4. 当前验收结论

代码侧迁移门禁继续完善，但当前数据库版本落后于代码四个 schema 版本，因此迁移尚未达到可扩大
切流的状态。先完成 schema 应用并让两个 drift gate 通过，再进入容量、SLO 和更多写路径切流。
