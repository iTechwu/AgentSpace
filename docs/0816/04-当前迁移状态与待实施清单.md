# 当前迁移状态与待实施清单

审查基线：`dev` 分支 `6d9b1ff9` 之后的当前工作树，2026-08-17。

## 1. 当前迁移状态

| 优先级 | 项目 | 状态 | 审查证据 |
| --- | --- | --- | --- |
| P0 | PostgreSQL schema 应用 | **已完成** | 通过允许入口 `pnpm --filter @dofe-agent/db run postgres:init` 完成 117→121；命令回报 `schemaVersion=121` |
| P0 | rollout reconcile schema | **已完成** | `skill_rollout_reconcile_item` 与 `idx_skill_rollout_reconcile_plan_status` 已在 dev 库存在；不变量门禁通过 |
| P1 | Prisma contract drift | **部分完成** | 已覆盖列、默认值、PK、unique/index、FK/onDelete |
| P1 | PostgreSQL invariant drift | **已完成（当前 schema）** | 比较对象、48 个 partial-index predicate、命名 CHECK 定义与 6 个函数体；enum/view 也已具备通用比较能力，当前库通过 |
| P1 | Prisma 切流 flag | **基础门禁完成** | 23 个域集中注册，未知/非法/不一致 flag fail-closed |
| P1 | Prisma 连接容量 | **配置完成、运行证据缺失** | role pool/timeout/application_name 有单测；缺少 Web/worker/daemon 压测与 waiting/timeout 指标 |
| P2 | shadow SLO / 回滚 | **已部分完成** | 快照持久化、周期 flush、跨实例加权、deadlock/P2034、burn-rate 与外部 pager 适配已实现；部署调度、真实 webhook 和发布适配器演练仍待完成 |
| P2 | 剩余写路径 | **已建立 inventory** | [05-Prisma剩余写路径inventory.md](./05-Prisma剩余写路径inventory.md) 记录 4 条已切流和 7 类待迁移高风险路径 |

## 2. 本轮已实施

1. 新增 `postgres:verify:invariants`，并接入 DB `pretest`。
2. 新增 invariant contract 提取与比较测试，覆盖 SQL 注释过滤和缺失对象诊断。
3. 修复 schema_version SQL 中 `\d` 模板转义错误，并将版本守卫测试改为跟随 `POSTGRES_SCHEMA_VERSION`；14 项守卫测试通过。
4. 通过 `postgres:init` 将 dev PostgreSQL 从 117 升至 121；随后 `prisma:verify:contract` 和 `postgres:verify:invariants` 均通过。
5. 新增集中 SLO 快照账本、周期 flush、跨实例加权汇总、deadlock/P2034 识别、burn-rate/pager 告警与发布回滚适配器契约；相关定向测试通过。
6. 将 invariant gate 扩展到 predicate、CHECK、enum、view 和 function body；当前库验证了 48 个 predicate 与 6 个函数体。
7. 新增剩余 Prisma 写路径 inventory，明确事务、幂等、事件与 legacy 退出条件。

## 3. 待实施顺序

1. **P1：建立连接池容量证据。** 分别记录 Web、worker、daemon 的 active/waiting/timeout、P95 和 graceful shutdown。
2. **P1：部署 SLO 周期任务。** 调度 `flushPrismaCutoverSloSnapshotsSync` 和 `sendPrismaCutoverSloPagerAlert`，传入不可变实例 ID，配置真实 webhook 并完成恢复演练。
3. **P1：接入发布系统适配器。** 将 `PrismaCutoverRollbackPublisher` 绑定真实发布系统，演练回退到 last-known-good flag 的权限、幂等和失败重试。
4. **P2：按 inventory 切流高风险写路径。** 先 task enqueue/outbox/lease，再处理需要跨表事务的 workflow 编排；每批必须满足 05 中的退出条件。

## 4. 当前验收结论

数据库已与代码 schema 121 对齐，Prisma contract 与扩展不变量 gate 均通过。可以继续准备单域
shadow/写切流，但仍不得扩大生产切流，直到连接池容量、实际告警路由、发布回滚演练及高风险写路径
事务对照完成。
