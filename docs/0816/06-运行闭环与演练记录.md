# 运行闭环与演练记录

记录时间：2026-08-17。代码基线：`b94242bb`。

## 已落地入口

| 项目 | 入口 | 当前状态 |
| --- | --- | --- |
| SLO flush + pager | `GET /api/cron/prisma-cutover-slo` | 已由 `deploy/self-hosted/runtime-maintenance.mjs` 每周期调度；Bearer `CRON_SECRET` 校验 |
| 归档与 retention | `archivePrismaCutoverSloSnapshotsToFileSync` | 归档 JSONL 写入成功后才删除过期行；未配置 `PRISMA_CUTOVER_SLO_ARCHIVE_DIR` 时 skipped |
| 外部 pager | `EXTERNAL_PAGER_WEBHOOK_URL` / `EXTERNAL_PAGER_TOKEN` | 代码、severity filter、active/cleared recovery 状态已完成；真实 webhook 未写入仓库 |
| 回滚发布 | `PRISMA_CUTOVER_ROLLBACK_WEBHOOK_URL` + `PRISMA_CUTOVER_RELEASE_ID` | HTTP publisher 已支持超时、Bearer token、幂等键、publicationId；自动回滚仍需显式开启 |
| 连接池容量 | `pnpm --filter @dofe-agent/db run prisma:pool:evidence` | 已能记录 web/worker/daemon 配置和 `pg_stat_activity` 观测 |

## Dev 采集快照

命令：

```text
pnpm --filter @dofe-agent/db run prisma:pool:evidence
```

采集时间：`2026-08-17T01:43:20.769Z`。配置上限为 Web=10、worker=5、daemon=3；三类
`application_name` 的 observed connections、active 和 waiting 均为 0。该结果证明采集查询和
角色映射可用，但当前没有长驻 Prisma 实例，不能作为容量或饱和阈值验收。下一次 Web/worker/daemon
压测必须同时保留该命令输出、请求并发、P95、连接超时、waiting 数和 graceful shutdown 日志。

## 隔离库容量压测入口

新增命令 `pnpm --filter @dofe-agent/db run prisma:pool:loadtest`。它用 PrismaPg 建立指定角色的
连接池，并发执行带 `pg_sleep` 的参数化查询。观察连接会在负载尚未完成时通过独立 PostgreSQL connection
查询 `pg_stat_activity`，报告 `observedConnections/activeConnections/waitingConnections`；同时输出
`connectionTimedOut`、`statementTimedOut`、查询 P95、`connectionWaitP95Ms`，并用独立 Prisma probe 在查询
in-flight 时调用 `$disconnect`，记录 `gracefulShutdownMs/gracefulShutdownDuringInflight`。CLI 只接受数据库名
包含 `test`、`loadtest` 或 `ci` 的显式 URL，防止误压普通 dev/生产库。

当前状态：代码与单测已完成，但本地没有可授权的隔离压测库，尚未产生 Web/worker/daemon 的真实 P95、
timeout、池耗尽和 graceful shutdown 报告；不得把上述 Dev 快照的 observed=0 当作容量验收。

## 告警恢复演练

自动化演练已覆盖：同一 alert 连续出现时 escalation、条件清除时 recovery payload、空周期不消费
待发送 recovery；定向测试为 `external-pager.test.ts` 和 `prisma-cutover-slo-pager.test.ts`。

真实演练仍待部署环境完成：

1. 在 secret store 写入真实 `EXTERNAL_PAGER_WEBHOOK_URL` 和 token，保留 `EXTERNAL_PAGER_SEVERITY_FILTER=warning,error`。
2. 注入一条超过阈值的 SLO 快照，确认外部系统收到 `prisma.cutover.slo.burn_rate`。
3. 清除快照条件，连续运行两次 cron，确认第一次发送 recovery、第二次不重复发送。
4. 保存 webhook 响应、pager state 行和 cron run evidence；失败时保持自动回滚关闭。

当前不能声称“真实 webhook 已配置”或“恢复演练已完成”，因为本地没有该外部系统的授权凭据和目标端点。

## 回滚发布演练

HTTP payload 固定包含 `releaseId`、`currentFlagVersion`、`targetFlagVersion`、`rollbackReasons`、
`burnRate` 和 `idempotency-key`。部署时先设置 `PRISMA_CUTOVER_AUTO_ROLLBACK_ENABLED=0` 做 dry-run，
验证发布系统权限、重复请求返回同一 publicationId、网络失败不重写 legacy 数据；通过后才允许在单一
workspace 开启自动回滚，并保留 last-known-good flag 的审计记录。

## Schema / gate

Dev 数据库已通过允许入口从 schema 121 升至 122。当前验证：

- `prisma:verify:contract`：26 models；
- `postgres:verify:invariants`：indexes=188、predicates=48、functions=6、triggers=5；
- `prisma:verify:raw-sql`：通过。
