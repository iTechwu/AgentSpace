# 0816 实施清单与验收矩阵

## P0：先消除规格与安全不确定性

| 编号 | 动作 | 验收证据 |
| --- | --- | --- |
| P0-1 | 重写 0815/07，标记历史审查，统一最终结论 | 单一状态表；每项链接提交、测试和命令 |
| P0-2 | 统一 04/05/06/07/08 的 `planId`、非唯一审批和 T14 语义 | 文档 grep 不再出现互相矛盾的 UNIQUE/非 UNIQUE 契约 |
| P0-3 | 明确 planner 与 dispatch 的 planId 责任边界 | 类型测试覆盖 planner、持久化记录和 dispatch result 的关联 |
| P0-4 | 定义批量派发失败后的 reconcile/consume 状态机 | 中途失败、重复重试、并发重试均有确定性结果 |

## P1：阻止错误切流或错误下发

| 编号 | 动作 | 验收证据 |
| --- | --- | --- |
| P1-1 | `all-compatible` 按 GPU、egress、MCP/CLI capability 过滤 | 在线但能力不匹配 Runtime 不进入 plan；全不匹配 fail-closed |
| P1-2 | 实现 full schema contract + SQL invariant gate | unique/index/FK/check/enum/trigger/function/view 漂移测试 |
| P1-3 | 为 Prisma client 增加角色化 pool/timeout/application_name 配置 | Web/worker/daemon 压测、连接池等待和 graceful shutdown 报告 |
| P1-4 | 建立集中 flag registry 和启动校验 | 未知 flag 启动失败；一次只允许 registry 授权的域切流 |
| P1-5 | 完成 cutover 指标聚合、SLO 和回滚演练 | mismatch/fallback/P95/连接池告警，演练可恢复到 legacy |

## P2：完成迁移闭环并降低维护成本

| 编号 | 动作 | 验收证据 |
| --- | --- | --- |
| P2-1 | 建立剩余写路径 inventory，优先 task enqueue/outbox/lease | 每个调用方有负责人、事务边界、幂等和事件顺序对照 |
| P2-2 | 为 raw SQL 建立 Unsafe 禁止和参数化审计门禁 | CI 扫描无 Unsafe；每条 SQL 有类型、事务和索引说明 |
| P2-3 | 同步 iteration_group 注释、规格和质量门输入 schema | 文档、代码注释、运行时审计事件和测试一致 |
| P2-4 | 补齐 rollout placement、能力过滤、失败恢复的跨 Runtime 测试 | 对应 05 T06/T09/T12/T14/T19 的可追溯测试矩阵 |

## 交付门槛

只有 P0 全部完成、P1-1~P1-5 有自动化证据后，才建议扩大 Prisma 生产切流或宣称
0815 方案“完整验收”。P2 可以分批交付，但必须在 progress log 中保留未完成项和预计
关闭条件；删除 legacy 路径前需满足 30 天零 drift、无未解释 fallback/mismatch、并完成
一次前向修复演练。
