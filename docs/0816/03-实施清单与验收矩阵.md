# 0816 实施清单与验收矩阵

## P0：先消除规格与安全不确定性

| 编号 | 动作 | 验收证据 |
| --- | --- | --- |
| P0-1 | **已完成**：重写 0815/07，区分历史审查与当前状态 | 单一当前结论、剩余风险和定向验证证据 |
| P0-2 | **已完成**：统一 04/05/06/07/08 的 `planId`、非唯一审批和 T14 语义 | 05 T14、07 当前状态、08 决策记录已采用非唯一 + consumedAt/planDigest 语义 |
| P0-3 | **已完成**：明确 planner 与 dispatch 的 planId 责任边界 | `SkillRolloutPersistedPlan` 与 dispatch 测试覆盖 planner、持久化记录和 result 关联 |
| P0-4 | **已完成（基础闭环）**：dispatch 失败返回 `planId/planDigest/createdInstallations`，并持久化 reconcile 状态 | `skill_rollout_reconcile_item` 覆盖 `pending/failed/created`、attempt/error/installation 引用；中途失败、Runtime 恢复重试、重复调用测试已通过；多进程并发与长时间退避仍需运行环境证据 |

已完成的增量：Prisma pool 配置边界测试已加入；rollout `planDigest` 已覆盖闭包项的
`parentArtifactDigest`，并有父节点变更回归测试。上述增量不改变 P0/P1 的剩余验收门槛。

## P1：阻止错误切流或错误下发

| 编号 | 动作 | 验收证据 |
| --- | --- | --- |
| P1-1 | **已部分完成**：`all-compatible` 按版本化 GPU、egress、MCP/CLI capability 过滤 | rollout 已按 `metadata.runtimeCapabilities` fail-closed，Runtime 注册/心跳已校验、规范化并绑定 `daemon_probe` provenance；仍需真实探针证明和跨 Runtime 验收 |
| P1-2 | **已完成（当前 schema）**：Prisma contract 与 PostgreSQL 定义级 invariant gate | `prisma:verify:contract` 通过；117→122 已经由 `postgres:init` 应用；`postgres:verify:invariants` 校验 indexes=188、48 个 predicate、CHECK 定义和 6 个函数体，并支持 enum/view 比较 |
| P1-3 | **已部分完成**：Prisma client 增加角色化 pool/timeout/application_name 配置，并提供容量证据 CLI | `prisma-client-config.test.ts` 与 `prisma:pool:evidence` 已通过；隔离库 `prisma:pool:loadtest` 已在负载期间采集连接、区分 connection/statement timeout、输出等待 P95，并执行 in-flight disconnect 探针；仍需 Web/worker/daemon 三角色真实报告 |
| P1-4 | **已完成（基础门禁）**：建立集中 flag registry 和 Prisma Client 初始化校验 | `cutover-flags.ts` 登记 25 个域；未知 flag、非法值、未注册 write、shadow/read 不一致均 fail-closed；`cutover-flags.test.ts` 已覆盖 |
| P1-5 | **已部分完成**：集中 cutover SLO、周期 flush、时间窗聚合、跨实例 burn-rate、pager、retention/archive 与回滚 HTTP 适配 | `/api/cron/prisma-cutover-slo` 已由 maintenance worker 调度；`idx_audit_log_code_created`、JSONL archive 和归档后删除已测试；真实 webhook、真实发布系统权限和恢复演练仍待部署执行 |

## P2：完成迁移闭环并降低维护成本

| 编号 | 动作 | 验收证据 |
| --- | --- | --- |
| P2-1 | **已部分完成**：建立 inventory、写 adapter，并接入 dispatcher node-level Prisma 原子事务 | [05 inventory](./05-Prisma剩余写路径inventory.md) 已记录 adapter、CAS/幂等测试；node.ready 已实现 run→node 锁顺序、P2034/40P01 有界重试、queue unavailable 退避、审批节点 legacy 回退和 outbox attempt 修正；flag 默认关闭，run-level fan-out、真实 deadlock 注入与 shadow 对照仍待完成 |
| P2-2 | **已部分完成**：为 raw SQL 建立 Unsafe 禁止门禁 | `prisma:verify:raw-sql` 已接入 DB `pretest`；仍需每条 SQL 的类型、事务和索引审计 |
| P2-3 | **已部分完成**：同步 iteration_group、审批配置和质量报告规格 | `qualityReportField` 已进入领域配置、编译标记和 coordinator fail-closed 校验；模板已要求显式 `approvalChannelName` 并拒绝空值；coordinator 已校验 manifest 中完整报告的 digest/Workspace/schema/subject/checks；仍需报告来源证明和审批权限审计断言 |
| P2-4 | **已完成（单进程证据）**：rollout placement、能力过滤、可选依赖、冲突和 reconcile 失败恢复已有定向测试 | `rollout.test.ts`、`skill-rollout-reconcile.test.ts`、`rollout-dispatch.test.ts` 已覆盖 placement/all-compatible/skipped/conflict、离线失败后恢复和重复 reconcile；仍需多进程并发/锁竞争的运行环境级验收 |

## 交付门槛

只有 P0 全部完成、P1-1~P1-5 有自动化证据后，才建议扩大 Prisma 生产切流或宣称
0815 方案“完整验收”。P2 可以分批交付，但必须在 progress log 中保留未完成项和预计
关闭条件；删除 legacy 路径前需满足 30 天零 drift、无未解释 fallback/mismatch、并完成
一次前向修复演练。
